# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Rapports Playwright (Tâche 5)
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Variables d'environnement (secrets) — ne jamais committer.
# Seul .env.example (avec des valeurs factices) doit être versionné.
.env
.env.*
!.env.example

# Dump de schéma temporaire (mise en place de l'environnement de staging —
# voir supabase/migrations/SETUP_STAGING.md), ne doit jamais être commité.
supabase/_*_schema_dump.sql

# Éviter la re-création accidentelle du dossier de migrations imbriqué (nettoyage du 04/07/2026)
supabase/migrations/migrations/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
